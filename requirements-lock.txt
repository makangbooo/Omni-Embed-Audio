# GPU-02 candidate direct-dependency lock (Linux x86_64, Python 3.11)
#
# This is intentionally not yet described as a complete transitive lock. The
# first successful A100 installation must preserve `pip freeze --all` and
# `conda env export`; those resolved files will be audited before this file is
# promoted to the final lock.
#
# PyTorch is installed separately by scripts/setup_environment.sh:
#   torch==2.7.1+cu126
#   torchvision==0.22.1+cu126
#   torchaudio==2.7.1+cu126

# [INFERRED] The base-model cards request the older Qwen2.5-Omni preview
# commit, but it cannot import the repository-required PEFT 0.18.0. The PEFT
# 0.18.0 official examples pin Transformers 4.52.4, which also contains the
# Qwen2.5-Omni implementation and modeling_layers required by PEFT.
transformers==4.52.4
tokenizers==0.21.4
huggingface-hub==0.36.0
safetensors==0.7.0
sentencepiece==0.2.1

# [CODE] Core training/runtime dependencies. Exact candidates follow the
# official lower bounds while avoiding unbounded future upgrades.
accelerate==1.10.1
peft==0.18.0
hydra-core==1.3.2
omegaconf==2.3.0
wandb==0.23.0

# Audio and numeric stack.
librosa==0.11.0
nnAudio==0.3.3
soundfile==0.13.1
numpy==2.1.3
pandas==2.2.3
scipy==1.15.3
scikit-learn==1.6.1

# Utilities imported by the repository.
tqdm==4.67.1
PyYAML==6.0.2
Pillow==11.3.0
requests==2.32.5
httpx==0.28.1
einops==0.8.1
timm==1.0.11
psutil==7.0.0

# CLI/type packages listed by the official requirements.
typer==0.20.0
pydantic==2.12.5
click==8.3.1

# Test tooling for the reproduction patches.
pytest==8.4.1

# FlashAttention is deliberately excluded from GPU-02. First validate the
# standard PyTorch SDPA path; add a CUDA-12.6-compatible flash-attn build in a
# separate, recorded step only if the official model smoke requires it.
